{
  "name": "deploy-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Central webhook and Docker rollout manager for small VPS-hosted webapps.",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test tests/deploy-app.test.mjs",
    "check": "node --check src/server.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
